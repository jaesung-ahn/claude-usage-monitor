# 설계 문서 (로컬 보관)
docs/

# macOS
.DS_Store

# Swift
.build/
.swiftpm/
*.xcuserstate

# 빌드 산출물
# .noindex 로 끝나면 Spotlight 가 색인하지 않는다.
# 개발 빌드가 응용 프로그램 검색에 섞이는 것을 막는다.
build.noindex/
build/
*.app
*.dmg

# Node
node_modules/

# Claude Code 로컬 설정
.claude/settings.local.json
